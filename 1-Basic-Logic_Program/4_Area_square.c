//W A P to find the Area of Square
#include<stdio.h>
main()
{
	int a,Area;
	printf("Enter the length of the Square :");
	scanf("%d",&a);
	
	//Formula to find the Area of square
	Area=a*a;
	
	printf("\nArea of the Square is %d",Area);
	
}
